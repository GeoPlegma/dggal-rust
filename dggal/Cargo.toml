[package]
name = "dggal"
version = "0.1.2"
edition = "2021"
description = "The dggal crate"
license-file = "LICENSE"

[lib]
name = "ecrt"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
ecrt-sys = { path = "../ecrt-sys" }
ecrt = { path = "../ecrt" }
dggal-sys = { path = "../dggal-sys" }
